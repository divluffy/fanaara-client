import React from "react";

const testframes = () => {
  return (
    <div>
      {/* <div className="relative" style={{ width: 90, height: 90 }}>
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <CosmicNebulaVipFrame size={125} className="absolute inset-0" />
      </div>
      <div className="relative h-20 w-20">
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <LightningChakraFrame size={125} className="absolute inset-0" />
      </div>
      <div className="relative" style={{ width: 82, height: 82 }}>
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <SakuraPetalsOrbitFrame
          size={125}
          className="absolute inset-0"
          size={82}
        />
      </div>
      <div className="relative h-20 w-20">
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <WaterBreathingWaveFrame size={125} className="absolute inset-0" />
      </div>
      <div className="relative" style={{ width: 86, height: 86 }}>
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <DragonAuraCoilFrame size={125} className="absolute inset-0" />
      </div>
      <div className="relative h-20 w-20">
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <MagicCircleRunesFrame
          size={125}
          className="absolute inset-0 text-cyan-300/90"
        />
      </div>
      <div className="relative h-20 w-20">
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <SwordSlashArcFrame
          size={125}
          className="absolute inset-0 text-white/90"
        />
      </div>
      <div className="relative inline-flex">
        <Avatar
          src={user?.avatar.md}
          alt={`${user?.username} avatar`}
          blurHash={user?.avatar?.blurHash}
          size="28"
          effects={false}
        />
        <ShipWheelFrame size={125} className="absolute inset-0" />
      </div> */}
    </div>
  );
};

export default testframes;
